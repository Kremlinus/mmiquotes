<%= form_for(@quote) do |f| %>
  <% if @quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
      <% @quote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :quote %><br>
    <%= f.text_area :quote, :class => 'new'%>
  </div>
  <div>
    <%= f.label :autor%><br>
    <%= f.text_field :autor, :class => 'new'%>
  </div>
  <div class="button-users">
    <%= f.submit%>
  </div>
<% end %>
